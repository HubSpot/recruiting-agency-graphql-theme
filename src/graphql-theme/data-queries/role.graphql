"""
  label: "Role Instance"
  description: "Role instance query for Recruiting Agency case study."
"""
query role($role_identifier: String! = "{{request.query_dict.role_identifier}}") {
  CRM {
    p_role(uniquePropertyName: "role_identifier", uniquePropertyValue: $role_identifier) {
      associations {
        company_collection__role_to_company {
          items {
            name
          }
        }
      }
      department
      description
      id
      location
      role_identifier
      salary_max
      salary_min
      title
      status
      desired_start_date
    }
  }
}