#  label: "Role Instance"
#  description: "Role instance query for Recruiting Agency case study."
#  $role_identifier: "{{ request.path_param_dict.dynamic_slug }}"
query role_dynamic($role_identifier: String!) {
  CRM {
    p_role(uniquePropertyName: "role_identifier", uniquePropertyValue: $role_identifier) {
      _metadata {
        id
      }
      associations {
        company_collection__role_to_company {
          items {
            name
          }
        }
      }
      department
      description
      location
      role_identifier
      salary_max
      salary_min
      title
      status
      desired_start_date
    }
  }
}